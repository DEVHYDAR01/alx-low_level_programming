#include <stdio.h>

/**
 * main - To print in both lower and uppercase
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char k;

	for (k = 'a'; k <= 'a'; k++)
	{
		putchar(k);
	}

	for (k = 'A'; k <= 'Z'; k++)
	{
		putchar(k);
	}
	return (0);
}
